import { makeStyles, Theme } from '@material-ui/core/styles';

/**
 * Mui styling for the footer container component.
 *
 * @exports StylesHook
 */
export default makeStyles((theme: Theme) => ({
  footer: {
    padding: theme.spacing(2),
  },
}));
