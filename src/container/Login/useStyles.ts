import { makeStyles } from '@material-ui/core/styles';

/**
 * Mui styling for the login container component.
 *
 * @exports StylesHook
 */
export default makeStyles({
  root: {
    minHeight: '500px',
    height: '100vh',
    display: 'flex',
    justifyContent: 'center',
    flexDirection: 'column',
  },
});
